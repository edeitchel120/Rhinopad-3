# SPDX-License-Identifier: MIT

build:
  # CMake and Kconfig files live next to this module.yml
  cmake: .
  kconfig: Kconfig

  # All paths here are relative to THIS file (zephyr/module.yml)
  settings:
    # Your shields live in ../boards/shields/rhinopad
    board_root: ../boards
    shield_root: ../boards
    kconfig_root: ../config
